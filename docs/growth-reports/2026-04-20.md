# Geck Inspect growth report — 2026-04-20

> **Setup incomplete.** Secrets needed before this report populates with real data:

- `GOOGLE_SERVICE_ACCOUNT_JSON`

Once all three secrets are configured in the repo Settings → Secrets and variables → Actions, the next Monday 11:00 UTC run will produce a full report with pulled Search Console and Google Analytics data.

See [the setup walkthrough in the Claude session](#) for step-by-step instructions, or run `node scripts/growth-report.mjs` locally with the same env vars exported to verify end-to-end.
