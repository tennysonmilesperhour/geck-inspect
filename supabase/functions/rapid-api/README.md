# rapid-api

Placeholder "hello world" function that was deployed from the Supabase
dashboard and never wired to anything in the app. It requires a valid
user JWT (verify_jwt is on) and only echoes the name it is sent.

It is tracked here so the repo matches what is deployed. Delete it from
the Supabase dashboard (Edge Functions, rapid-api, Delete) once you are
sure nothing external calls it.
